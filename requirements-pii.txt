# Optional Presidio PII-detection tier (Phase 1, S1.4).
#
# NOT installed in the Hugging Face image: presidio-analyzer pulls spaCy, which
# requires thinc>=8.3.12 (Python >=3.10,<3.15) — no installable build exists on
# the HF runtime, so adding it to requirements.txt breaks the deploy.
#
# src/contract/ingest_gate.py detects Presidio at runtime and falls back to a
# regex PII tier when it is absent, so omitting this file yields a correct
# (lower-recall) deployment. Install it only where Python is within spaCy's
# supported range:
#
#     pip install -r requirements-pii.txt
#     python -m spacy download en_core_web_lg
#
-r requirements.txt
presidio-analyzer==2.2.359
presidio-anonymizer==2.2.362
