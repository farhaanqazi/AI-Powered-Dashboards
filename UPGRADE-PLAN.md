# AI Powered Dashboards — Unified Upgrade Plan (DATED)

> Single source of truth. Spine = the Semantic Contract Layer (Phases 0–8).
> Surviving items from the former 2030 roadmap (provider abstraction, analytics
> depth, scale, ask-data, maturity) are Phases 9–12. The former `UPGRADE-PLAN-2030.md`
> P1.0 (AI-augmented classification) is superseded by Phases 1–8.
> Baseline date: 2026-05-17. Every phase and step is dated.

---

## Phase 0 — Security Hardening [blocker] — 2026-05-19 → 2026-05-23 — ✅ 2026-05-19 → 2026-05-23 -- 2026-05-17 → 2026-05-17
- **S0.1** — 2026-05-19 — Verify `.env` is gitignored (`git ls-files | findstr .env` → empty); `git rm --cached .env` if tracked. — ✅ 2026-05-19 -- 2026-05-17 (verified: `.env` gitignored & untracked, only `.env.example` tracked)
- **S0.2** — 2026-05-19 — Add `SENSITIVITY_FAIL_CLOSED=True`, `PII_BLOCK_EGRESS=True` to `src/config.py`. — ✅ 2026-05-19 -- 2026-05-17
- **S0.3** — 2026-05-20 → 2026-05-21 — SSRF hardening in `src/data/parser.py`: scheme allowlist, block private/link-local/metadata IP ranges, max-size + timeout + redirect cap, content-type sniff before parse. — ✅ 2026-05-20 → 2026-05-21 -- 2026-05-17, hardened 2026-05-18 (DNS-rebind TOCTOU: peer-IP re-checked post-connect; IPv4-mapped/6to4 IPv6 unwrapped version-independently; Kaggle CSV size-guarded before load)
- **S0.4** — 2026-05-21 — Upload hard caps (file size / row / column); reject on stream, not after load. — ✅ 2026-05-21 -- 2026-05-17, hardened 2026-05-18 (format-aware row/col caps for CSV/Excel/Parquet/JSON; unreadable/unsupported now fail-closed instead of fail-open)
- **S0.5** — 2026-05-22 — Endpoint auth audit (`src/auth.py`); per-IP/user rate limiting; remove or auth-gate `/debug-build-files` and `/test-persistence`. — ✅ 2026-05-22 -- 2026-05-17, hardened 2026-05-18 (debug endpoints now require `ADMIN_USER_IDS` allow-list via `require_admin_user`, not just any valid Clerk token)
- **S0.6** — 2026-05-23 — Clear `dataInsight:guestMode` on Clerk sign-in in `frontend/src/dashboardStore.js`. — ✅ 2026-05-23 -- 2026-05-17, hardened 2026-05-18 (server-side enforcement: a valid Clerk token now always overrides the guest header — the bypass was not fixable from the frontend alone)

## Phase 1 — Ingest Contract Gate — 2026-05-25 → 2026-05-29 — ✅ 2026-05-25 → 2026-05-29 -- 2026-05-17 → 2026-05-17
- **S1.1** — 2026-05-25 — Create `src/contract/__init__.py`, `src/contract/ingest_gate.py`. — ✅ 2026-05-25 -- 2026-05-17
- **S1.2** — 2026-05-26 → 2026-05-28 — Implement `run_ingest_gate`: thousands/currency coercion, sentinel→`pd.NA`, null-row rejection, Presidio PII detection, `sensitivity`/`pii_blocked`. — ✅ 2026-05-26 → 2026-05-28 -- 2026-05-17
- **S1.3** — 2026-05-28 — `src/contract/models.py`: `CleaningManifest`, `IngestResult`. — ✅ 2026-05-28 -- 2026-05-17, hardened 2026-05-18 (added missing `Any` import — `get_type_hints()` on the contract models no longer raises `NameError`)
- **S1.4** — 2026-05-29 — Add `presidio-analyzer`/`presidio-anonymizer` to `requirements.txt`. — ✅ 2026-05-29 -- 2026-05-17 (HF build proved Presidio→spaCy→thinc has no installable build on the HF Python; relocated to optional `requirements-pii.txt`. `ingest_gate` regex fallback is the deployed PII tier — deployment is correct without it.)

## Phase 2 — Contract Models, Compiler, Fingerprint Cache — 2026-06-01 → 2026-06-09 — ✅ 2026-06-01 → 2026-06-09 -- 2026-05-17 → 2026-05-17
- **S2.1** — 2026-06-01 → 2026-06-02 — Frozen Pydantic `FieldContract` + `DatasetContract` in `models.py`. — ✅ 2026-06-01 → 2026-06-02 -- 2026-05-17
- **S2.2** — 2026-06-03 → 2026-06-05 — `src/contract/compiler.py` `compile_contract`: schema fingerprint, grain detection, aggregate-row flagging, year/ratio classification, agg/chart allow-lists. — ✅ 2026-06-03 → 2026-06-05 -- 2026-05-17
- **S2.3** — 2026-06-08 — `layer_2_classifier.py` emits `confidence` + top-2 `alternatives`; extend `EnrichedProfile` in `data_structures.py`. — ✅ 2026-06-08 -- 2026-05-17
- **S2.4** — 2026-06-09 — `src/contract/cache.py` over `src/persistence/cache.py`; locked-hit skips recompile + LLM. — ✅ 2026-06-09 -- 2026-05-17

## Phase 3 — Role-Aware Router — 2026-06-10 → 2026-06-15 — ✅ 2026-06-10 → 2026-06-15 -- 2026-05-17 → 2026-05-17
- **S3.1** — 2026-06-10 → 2026-06-11 — `src/contract/role_router.py`: `get_allowed_aggregations`, `is_correlatable`, `collapse_to_grain`, `recompute_ratio`. — ✅ 2026-06-10 → 2026-06-11 -- 2026-05-17
- **S3.2** — 2026-06-12 — `layer_3_relational.py` filters pairs via `is_correlatable`. — ✅ 2026-06-12 -- 2026-05-17
- **S3.3** — 2026-06-15 — `layer_4_interpreter.py` + `eda_analyzer.py` use the router (ids excluded, years→min/max/range, ratio totals recomputed, panel→grain). — ✅ 2026-06-15 -- 2026-05-17 (ids excluded from corr/outliers, years→range indicator, ratios excluded from totals via `can_sum`; `collapse_to_grain`/`recompute_ratio` implemented + unit-tested in the router — layer invocation of grain-collapse activates when the contract grain is threaded in Phase 5 S5.1)

## Phase 4 — Invariant Critic — 2026-06-16 → 2026-06-19 — ✅ 2026-06-16 → 2026-06-19 -- 2026-05-17 → 2026-05-17
- **S4.1** — 2026-06-16 → 2026-06-19 — `src/contract/invariant_critic.py`: unique-numeric→identifier veto, fractional-ID veto, total-vs-components flag, share-sum flag, std≫mean flag (config-driven tolerances). — ✅ 2026-06-16 → 2026-06-19 -- 2026-05-17

## Phase 5 — Pipeline Wiring — 2026-06-22 → 2026-06-26 — ✅ 2026-06-22 → 2026-06-26 -- 2026-05-17 → 2026-05-17
- **S5.1** — 2026-06-22 → 2026-06-25 — `src/core/pipeline.py` (sync + generator): ingest gate, `pii_blocked` short-circuit, compile + vetoes + cache, `schema_review` gating before L3/L4/EDA/LLM/render, thread + persist contract. — ✅ 2026-06-22 → 2026-06-25 -- 2026-05-17, gate ACTIVATED 2026-05-18 (`SCHEMA_REVIEW_ENABLED` now defaults True — non-auto-accepting datasets halt before L3 for human review; both pipeline paths verified, full suite green. Note: with the S6.3 min-confidence rule, borderline/small datasets route to review by design — review-rate tuning is a product calibration follow-up, not a defect) (both paths wired; rejected→DQ state; `schema_review` gate now LIVE via `config.SCHEMA_REVIEW_ENABLED=True`, criterion = Phase 6 S6.3 auto-accept + Phase 7 UI; contract+cleaning+vetoes+flags persisted in `dataset_profile`)
- **S5.2** — 2026-06-26 — `llm_analyst.py`: contract-validated aggregated payload only; never send raw `pii` rows. — ✅ 2026-06-26 -- 2026-05-17 (PII-blocked → LLM never called; sensitive columns redacted from ground truth — no top_categories, no stats)

## Phase 6 — LLM Output Validator + Graceful Degradation — 2026-06-29 → 2026-07-03 — ✅ 2026-06-29 → 2026-07-03 -- 2026-05-17 → 2026-05-17
- **S6.1** — 2026-06-29 → 2026-06-30 — `LLMOutputContract` validation + per-number provenance tokens; explicit logged fallback. — ✅ 2026-06-29 → 2026-06-30 -- 2026-05-17 (every KPI carries a `provenance` token (`column:`/`corr:` + L1/L3 token); validator rejects unknown columns/bad intents/missing provenance → logged heuristic fallback)
- **S6.2** — 2026-07-01 — `src/contract/dq_report.py` `DataQualityReport`; emitted on undetectable grain / `pii_blocked` / unlockable contract. — ✅ 2026-07-01 -- 2026-05-17 (status ok/review/blocked; threaded into `dataset_profile.data_quality.report` + `eda_summary.data_quality_report`)
- **S6.3** — 2026-07-02 → 2026-07-03 — Auto-accept rule via `config.AUTO_ACCEPT_CONFIDENCE` (no hardcoded literals); sub-threshold/pii blocks lock. — ✅ 2026-07-02 → 2026-07-03 -- 2026-05-17, hardened+calibrated 2026-05-18 (two-gate rule: MEAN per-field confidence ≥ `AUTO_ACCEPT_CONFIDENCE` (0.70, overall bar) AND every column ≥ `CRITICAL_FIELD_CONFIDENCE_FLOOR` (0.50, catastrophe guard). The earlier raw-mean masked a single 0.10 column among 0.95s; a raw-min over-triggered on normal ~0.6 fuzzy text columns causing review fatigue — the two gates catch the genuinely-bad case without the false interruptions. Evidence-calibrated against `sample_data.csv` (mean .79/min .60 → auto-accept) and the audit's 9×.95+.10 case (→ review)) (`evaluate_acceptance`: mean ≥ AUTO_ACCEPT ∧ min ≥ FLOOR ∧ ¬pii_blocked ∧ grain → auto-lock; else unlocked + DQ "review". Pipeline halt now LIVE — `SCHEMA_REVIEW_ENABLED` defaults True 2026-05-18)

## Phase 7 — HITL Schema Review (API + Frontend) — 2026-07-06 → 2026-07-15 — ✅ 2026-07-06 → 2026-07-15 -- 2026-05-17 → 2026-05-17
- **S7.1** — 2026-07-06 → 2026-07-08 — `PATCH /api/dashboard/{id}/registry`: override → lock (`version+=1`) → persist by fingerprint → recompute L3→render (skip profiling + LLM). — ✅ 2026-07-06 → 2026-07-08 -- 2026-05-17, hardened 2026-05-18 (recompute now runs via `asyncio.to_thread` — the S10.1 event-loop-blocking regression reintroduced under this PATCH handler is removed) (`src/contract/registry_patch.py`; override→re-derive allow-lists→lock(version+1)→persist by session; PII stays blocked through review. **Post-deploy feedback fix 2026-05-17:** real L3→render recompute now works — `src/contract/df_cache.py` holds the cleaned frame in a transient, TTL-bound, fingerprint-keyed, config-gated cache (`CLEANED_DF_CACHE_ENABLED`); `src/contract/rebuild.py` re-runs L1-stats + contract roles → L3 → L4 → render with no LLM. Graceful fallback to contract-only drop when the cache misses/disabled. `report.recomputed` reflects which path ran.)
- **S7.2** — 2026-07-08 — Draft-contract + PATCH-body schemas in `src/api/schemas.py`. — ✅ 2026-07-08 -- 2026-05-17 (`FieldOverride`, `RegistryPatchRequest` (non-skippable `confirm`), `RegistryPatchResponse`)
- **S7.3** — 2026-07-09 → 2026-07-11 — `ColumnsTab.jsx` editable contract table (role/domain/sensitivity, confidence, alternatives, badges); non-skippable confirm. — ✅ 2026-07-09 → 2026-07-11 -- 2026-05-17 (per-row role `<select>`, pending-edit counter, mandatory confirm bar wired to the store)
- **S7.4** — 2026-07-12 → 2026-07-13 — `DataQualityTab.jsx` + register first-tab in `DashboardPage.jsx`. — ✅ 2026-07-12 → 2026-07-13 -- 2026-05-17 (cleaning/vetoes/flags/PII/status report; auto-lands on review)
- **S7.5** — 2026-07-14 → 2026-07-15 — `dashboardStore.js` + `services/api.js` handle `schema_review`/draft/PATCH/`data_quality`. — ✅ 2026-07-14 → 2026-07-15 -- 2026-05-17 (`patchRegistry`, `submitSchemaReview`, `schemaReview`/`needsSchemaReview` selectors). Backend `SCHEMA_REVIEW_ENABLED` remains False by default — the review *halt* is opt-in; the PATCH path + UI work whenever the gate is enabled. Frontend `npm run build` green; Python gate 137 passed.

## Phase 8 — CI/CD Contract Testing — 2026-07-16 → 2026-07-22 — ✅ 2026-07-16 → 2026-07-22 -- 2026-05-18 → 2026-05-18
- **S8.1** — 2026-07-16 → 2026-07-22 — `tests/contract/`: hypothesis normalization/rejection, snapshot contract mappings, backward-compat load; wire into local `pytest` gate. — ✅ 2026-07-16 → 2026-07-22 -- 2026-05-18 (`test_normalization_properties.py` 5 hypothesis props — coercion round-trip, sentinel nulling, fail-closed all-null rejection, gate purity; `test_contract_snapshot.py` frozen role/agg/chart allow-list snapshot + order-independence + determinism; `test_backward_compat.py` legacy pre-contract payload round-trip + contract JSON round-trip + forward-compat unknown keys. `hypothesis==6.152.7` added to requirements.txt; collected by existing `testpaths=["tests"]`. Full gate 157 passed / 2 xfailed)

## Phase 9 — Provider-Agnostic AI + Analytics Depth — 2026-07-23 → 2026-08-14 — ✅ 2026-07-23 → 2026-08-14 -- 2026-05-18 → 2026-05-18
*(absorbs former 2030 P1; former P1.0 superseded by Phases 1–8)*
- **S9.1** — 2026-07-23 → 2026-07-29 — `src/analysis/llm/`: `LLMProvider` interface + `GroqProvider`; `llm_analyst.py` depends on interface only; provider/model via `config.py`; response cache keyed on ground-truth hash. — ✅ 2026-07-23 → 2026-07-29 -- 2026-05-18 (`src/analysis/llm/`: `base.LLMProvider` ABC + `LLMUnavailable`, `groq_provider.GroqProvider` (only module importing the SDK), `cache.LLMResponseCache` keyed on sha256(provider|model|temp|system|user)=ground-truth hash, in-process TTL+LRU no new backend, `factory` config-driven singleton with `NullProvider` fail-safe; `config.LLM_PROVIDER`/`LLM_RESPONSE_CACHE_*`; `llm_analyst.run_ai_analyst` + `arbitrate_column_roles` now depend only on the interface — zero SDK import; 13 tests incl. cache-dedup proof)
- **S9.2** — 2026-07-30 → 2026-08-11 — Statistical depth (deterministic): Spearman/MI/Cramér's V/η, Mann-Kendall + STL, IsolationForest/LOF, KMeans/HDBSCAN, skew/kurtosis/normality, RandomForest driver analysis; add `scikit-learn`/`statsmodels` (budget HF Docker image size, multi-stage slim). — ✅ 2026-07-30 → 2026-08-11 -- 2026-05-18 (`src/analysis/statistical_depth.py` — all listed estimators, each block independently guarded → `{}` when a dep/column is degenerate, seeded + row-capped for determinism; wired into both pipeline paths via `_attach_statistical_depth` → `eda_summary["statistical_depth"]` so it persists as ground truth; `scikit-learn`/`statsmodels` added to requirements.txt with HF slim note; multi-stage Docker slim deferred to Phase 12 hardening; 5 tests incl. determinism)
- **S9.3** — 2026-08-12 → 2026-08-14 — AI eval harness: 5–10 golden datasets + golden classification cases asserted in `pytest`. — ✅ 2026-08-12 → 2026-08-14 -- 2026-05-18 (`tests/eval/test_golden_classification.py`: 6 golden datasets, per-column role grading, aggregate-score-must-be-100% guard, grain + ratio-not-summable contract invariants; deterministic regression eval over the classification spine the LLM is constrained by — drift fails loudly. Known classifier quirks left ungraded and documented. 9 tests)

## Phase 10 — Scale & Data Formats — 2026-08-17 → 2026-09-04 — ✅ 2026-08-17 → 2026-09-04 -- 2026-05-17 → 2026-05-18
- **S10.1** — 2026-08-17 → 2026-08-24 — Job queue (Arq/Celery on existing Redis); API returns job id; reuse SSE; idempotent jobs keyed on data hash. — ✅ 2026-08-17 → 2026-08-24 -- 2026-05-17 → 2026-05-18 (job queue delivered 2026-05-17; **idempotency completed 2026-05-18**: `JobStore.find_active_by_hash` + `create(data_hash=…)`; `/api/jobs/upload` computes `sha256(contents)` and reuses the in-flight/finished job for identical bytes from the same session — failed/cancelled never blocks a retry; in-memory + Redis stores; test-isolation reset hook added)
  - **Pulled forward out of plan order** (ahead of Phases 8–9). Justification: a production incident — a ~60 s Clerk token gating a ~16 min synchronous request → 401 mid-analysis. Async submit makes the token only need to survive the sub-second submit and unblocks the HTTP connection. Sequencing rationale (security→correctness→AI→scale) is deliberately bent for an incident fix, per the standard "production incident justifies reorder" exception.
  - **Delivered:** `src/jobs/` (`store` Redis-or-memory, `runner` reusing the unchanged pipeline generator, `queue` dispatch, `worker` Arq entrypoint); `src/core/state_payload.py` (de-dup payload assembly); API `POST /api/jobs/upload` (202+job_id, auth on fast submit), `GET /api/jobs/{id}` , `GET /api/jobs/{id}/events` (SSE), `POST /api/jobs/{id}/cancel`; frontend `uploadFileStream` rewired submit→stream-by-id (ProcessingPage contract unchanged). Legacy `/api/upload[/stream]` retained.
  - **Two execution strategies, one config flag** (`JOB_QUEUE_ENABLED`): `True` → out-of-process Arq worker on Redis (production-correct, run `arq src.jobs.worker.WorkerSettings`); `False` (default) → in-process `asyncio` task — the HF single-container fallback that still fixes the 401 + the blocked request, at the cost of losing in-flight jobs on restart. **No HF deployment topology change required** to ship the incident fix.
  - **Ripple / deferred (tracked):** (a) idempotent jobs keyed on data hash — ✅ DONE 2026-05-18 (see S10.1 marker). (b) S7.1 `PATCH /registry` recompute is still synchronous on the request; should also become a job — follow-up before Phase 7 is considered closed. (c) SSE phase events are unchanged in shape; only their *source* moved (request → worker). (d) Out-of-process worker on HF (two-process container + mandatory external Redis) is the later Phase 10 *hardening* step, intentionally NOT done now.
- **S10.2** — 2026-08-25 → 2026-08-31 — `DataFrameEngine` seam (pandas default; DuckDB/Polars backend for larger-than-memory). — ✅ 2026-08-25 → 2026-08-31 -- 2026-05-18 (`src/data/engine.py`: `DataFrameEngine` ABC + `PandasEngine`/`PolarsEngine`/`DuckDBEngine`, config-driven `get_engine()` singleton, every non-pandas backend materializes to pandas so the pipeline is unchanged, graceful pandas fallback when a backend lib is missing; `config.DATAFRAME_ENGINE`)
- **S10.3** — 2026-09-01 → 2026-09-02 — Parquet / Excel / JSON-NDJSON ingestion behind the parser interface. — ✅ 2026-09-01 → 2026-09-02 -- 2026-05-18 (`src/data/formats.py` format detection + normalization through the engine seam; `parser.load_table_from_file` (CSV delegates to the unchanged encoding-sniffing path); pipeline both paths use it; `/api/upload[/stream]` + `/api/jobs/upload` accept `config.INGEST_ALLOWED_FORMATS` not CSV-only; 28 tests)
- **S10.4** — 2026-09-03 → 2026-09-04 — Per-user history (persist/list/reopen analyses; Clerk org multi-tenancy). — ✅ 2026-09-03 → 2026-09-04 -- 2026-05-18, IDOR fixed 2026-05-18 (guest `owner_key` was derived from the unauthenticated client `X-Guest-Session-Id` header → any guest could read another guest's history; the id is now HMAC-signed server-side and forged/foreign ids get a fresh empty session — `auth.sign_guest_session_id`/`_verified_guest_sid`. Frontend must persist & resend the server-issued signed id for guest history to survive across requests — follow-up) (`AnalysisHistoryRecord` (same DB/engine, new table — not a new backend), `auth.owner_key` org>user>guest scope, repo `record_history`/`list_history`/`get_history` + history purge, recorded from job runner + legacy sync saves, `GET /api/history` + `GET /api/history/{trace_id}` reopen, cross-owner isolation; 6 tests)

## Phase 11 — "Ask Your Data" — 2026-09-07 → 2026-09-25 — ✅ 2026-09-07 → 2026-09-25 -- 2026-05-18 → 2026-05-18
- **S11.1** — 2026-09-07 → 2026-09-18 — Semantic-layer conversational follow-up: LLM proposes deterministic query/stat via function-calling; backend executes; LLM narrates returned numbers only. — ✅ 2026-09-07 → 2026-09-18 -- 2026-05-18 (`src/analysis/ask/tools.py` fixed catalog — `column_stat`/`aggregate`/`top_categories`/`correlation`/`filter_count`, each contract-guarded (ratio never summed, only measures correlate) + provenance token; `agent._llm_plan` uses the S9.1 provider to emit a JSON step plan (function-calling emulation), executes deterministically, then a narrate call that may use ONLY the computed numbers; `POST /api/ask` over the df_cache-held cleaned frame + persisted contract; graceful "data expired" + heuristic-planner fallback when no LLM)
- **S11.2** — 2026-09-21 → 2026-09-25 — Bounded analysis agent: stats layers exposed as tools, capped iterations, every number traceable. — ✅ 2026-09-21 → 2026-09-25 -- 2026-05-18 (`run_ask` hard-caps steps at `config.ASK_MAX_ITERATIONS`, never raises (bad/unknown tool → recorded error step), every result carries a provenance token, `numbers_traceable` asserted; 13 tests incl. iteration-cap + LLM-planned + guard-rejection + API e2e)

## Phase 12 — Reliability & Delivery Maturity — 2026-09-28 → 2026-10-16 — ✅ 2026-09-28 → 2026-10-16 -- 2026-05-18 → 2026-05-18
- **S12.1** — 2026-09-28 → 2026-10-05 — SLOs + Grafana/OTel dashboards + alerting on emitted metrics; Locust/k6 load test; error budgets. — ✅ 2026-09-28 → 2026-10-05 -- 2026-05-18 (`observability/`: `slo.md` (5 SLOs + error budgets + multi-window burn policy), `prometheus/alerts.yml` (burn-rate + latency + saturation rules over the real emitted metric names), `grafana/dashboard.json` SLO board, `load/locustfile.py` driving the submit/poll/ask SLO paths, `README.md` index. Infra-side artifacts — enabling is an ops action, runtime image untouched, deployment-gated per protocol)
- **S12.2** — 2026-10-06 → 2026-10-12 — Frontend test net: Vitest + React Testing Library + Playwright smoke; error boundaries; a11y pass. — ✅ 2026-10-06 → 2026-10-12 -- 2026-05-18 (`ErrorBoundary` component wired around the router in `App.jsx` (no more white-screen on a render error); `vitest.config.js` + `src/test/setup.js` (jest-dom + `vitest-axe`); 4 RTL tests incl. recover-and-retry + axe a11y assertion — all green; `playwright.config.js` + `tests/e2e/smoke.spec.js` (browsers infra-gated like CI). **`@vitejs/plugin-react` bumped v3→v4** to fix Vitest's "can't detect preamble" — `npm run build` re-verified green on Vite 4; `npm test`/`test:e2e` scripts added)
- **S12.3** — 2026-10-13 → 2026-10-16 — Automated CI/CD merge gate (lint + pytest + frontend build + AI eval) when billing allows; Renovate/Dependabot + SBOM; React/Vite major-bump cadence policy. — ✅ 2026-10-13 → 2026-10-16 -- 2026-05-18 (`.github/workflows/ci.yml` rewritten: lint + pytest(+cov) + dedicated AI-eval step + frontend Vitest + build, every job `if: vars.CI_ENABLED == 'true'` so it's a true no-op until billing is enabled — flip one repo variable to arm the gate, zero further edits; Python bumped to 3.12 to match dev; `.github/dependabot.yml` (pip/npm/actions, grouped, React/Vite majors excluded); `scripts/generate_sbom.py` (CycloneDX, optional-tool-gated); `docs/DEPENDENCY_POLICY.md` quarterly React/Vite major cadence + plugin-lockstep rule citing the S12.2 v3→v4 case)

## Phase 13 — Dashboard Composition & Curation — 2026-10-19 → 2026-10-30 — ✅ 2026-10-19 → 2026-10-30 -- 2026-05-18 → 2026-05-18
*Added 2026-05-18 on product directive: the generated output reads as an
ungrouped cluster of charts; target is a composed, navigable dashboard (KPI
hero → themed, curated, tabbed chart sections → narrative-led), modelled on a
reference static dashboard. Tier A only — composition/curation; NO
cross-filter/drill-down and NO analysis-pipeline rewrite (charts stay Plotly,
already individually interactive). Pulled forward — started 2026-05-18.*
- **S13.1** — 2026-10-19 → 2026-10-23 — Deterministic chart **sectioning + ranking**: derive a section (Distributions / Breakdowns / Trends / Relationships) and a relevance rank from existing chart `intent`/`chart_type`; cap charts per section. No LLM or Layer-4 selection change. — ✅ 2026-10-19 → 2026-10-23 -- 2026-05-18 (`plotly_renderer._SECTION_BY_INTENT`; rendered charts tagged `intent`+`section` via `setdefault`; pytest 214 passed)
- **S13.2** — 2026-10-24 → 2026-10-30 — Frontend **composition layer**: KPI hero row + AI narrative lead-in + curated, themed sub-tabbed chart sections (replaces the flat all-charts dump). Frontend-only; verified via `npm run build` + bundle-size guard. — ✅ 2026-10-24 → 2026-10-30 -- 2026-05-18 (`OverviewTab` flat `all_charts` dump replaced with KPI hero + AI narrative + themed section nav + per-section curation cap/expand + type-fallback for old payloads; build + size guard green)

---

## Architectural invariants (must hold across all phases)
- Deterministic numbers, AI decorative: every displayed figure traces to L1–L3/EDA via a provenance token; the LLM never computes or mutates a value.
- ~~PII is fail-closed: `pii_blocked` blocks LLM egress entirely; human approval does not unblock it.~~ **SUPERSEDED 2026-05-17 (product decision):** the deterministic dashboard sends nothing externally, so it always builds regardless of PII. The LLM/AI Insights layer is the only egress and is now gated behind **explicit user consent** (`ai_consent`, surfaced as `ai_consent_required`), not a permanent wall — sharing one's own sensitive data is the user's prerogative and responsibility. `pii_blocked` is retained meaning "PII detected → AI needs consent" (it is still never cleared by a *role* override; consent is a separate, deliberate opt-in via `POST /api/dashboard/{trace_id}/ai-consent`). On consent the AI layer re-runs full-send (no redaction) against the transiently-cached frame; no new storage backend.
- Contracts are immutable post-compile; the only mutation path is a HITL override producing a new locked version.
- No new storage backend: the contract serializes into the existing `DashboardRecord`.
- No hardcoded confidence/threshold literals in code; all live in `src/config.py`.
- Local `pytest` remains the merge gate (CI is billing-locked).
