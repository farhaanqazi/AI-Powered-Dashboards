fastapi==0.109.2
uvicorn[standard]==0.27.0.post1
PyJWT[crypto]==2.8.0
python-dotenv==1.0.1
pandas==2.2.0
# Multi-format ingestion: INGEST_ALLOWED_FORMATS ships parquet/xlsx/xls by
# default, so pandas needs these IO backends. Absent here previously => pandas
# raised ModuleNotFoundError on .parquet/.xlsx uploads in the deployed image;
# it only ever "worked" because local dev venvs had them installed ad hoc.
openpyxl==3.1.5   # .xlsx read/write
pyarrow==21.0.0   # .parquet read/write (has cp39 manylinux + win wheels)
# DuckDB powers robust CSV dialect sniffing (delimiter/quote/type auto-detect)
# so ;/tab/|-separated uploads aren't misread as one comma column. Pinned <1.5
# because 1.5.0 dropped Python 3.9 (the deployed runtime); 1.4.x is the last
# 3.9 line. Optional at runtime — the reader falls back to pandas if absent.
duckdb>=1.4,<1.5
plotly==5.18.0
# Server-side PDF export (replaces the flaky in-browser html-to-image screenshot
# that produced blank pages). Pure-Python, ships a cp39 manylinux wheel (no
# compile, no system libs, no headless browser) and renders charts as native
# vector graphics — ~3 MB, negligible next to the ML stack. See src/reporting.
reportlab==4.0.9
kagglehub==0.2.2
scipy==1.12.0
# Phase 9 S9.2 statistical depth. src/analysis/statistical_depth.py degrades
# each block to {} when these are absent, so the image is still correct without
# them — but they are wanted in the deployed feature set. Budget the HF image
# via a multi-stage slim build (Phase 12 hardening) rather than dropping them.
scikit-learn==1.6.1
statsmodels==0.14.6
requests==2.31.0
chardet==5.2.0
python-multipart==0.0.6
jinja2==3.1.3
psutil==5.9.8
pytest==8.0.0
pytest-asyncio==0.23.5
pytest-cov==4.1.0
hypothesis==6.141.1
httpx==0.27.0
structlog==24.1.0
prometheus-client==0.20.0
opentelemetry-api==1.24.0
opentelemetry-sdk==1.24.0
opentelemetry-instrumentation-fastapi==0.45b0
opentelemetry-exporter-otlp-proto-http==1.24.0
# opentelemetry-instrumentation 0.45b0 imports pkg_resources, which setuptools
# >=81 no longer ships. Pin setuptools below 81 so the OTel stack imports.
setuptools<81
sentry-sdk[fastapi]==2.60.0
SQLAlchemy==2.0.29
alembic==1.13.1
psycopg2-binary==2.9.9
redis==4.6.0
arq==0.26.0
groq==1.0.0
slowapi==0.1.9
# PII detection: Presidio is an OPTIONAL extra (see requirements-pii.txt).
# It drags in spaCy/thinc, which has no installable build on the HF runtime's
# Python — installing it here breaks the deploy. src/contract/ingest_gate.py
# degrades gracefully to a regex PII tier when Presidio is absent, so the
# deployed image is correct without it. Install requirements-pii.txt only in
# environments whose Python is in spaCy's supported range.
