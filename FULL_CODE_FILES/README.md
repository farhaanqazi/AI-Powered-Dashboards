# FULL_CODE_FILES Export

This directory mirrors every tracked file in the repository as a standalone copy. Use these files when a separated, easily identifiable code listing is needed without scrolling through a monolithic document.
